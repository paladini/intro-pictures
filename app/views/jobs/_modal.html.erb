<div class="vertical-alignment-helper">
  	<div class="modal-dialog modal-lg vertical-align-center" role="document">
	    <div class="modal-content">
	      <div class="modal-body">

	      	<!-- Extra Small / Small devices -->
	      	<div class="row hidden-md hidden-lg">

	      		<!-- Meta information -->
	      		<div class="col-xs-12 col-video-description">
	          	<div class="row meta">
	          		<div class="col-xs-10">
	          			<p class="video-title">
		          			<%= if I18n.locale == :en 
		          					@job.title_en
		          				elsif I18n.locale == :pt
		          					@job.title_pt
		          				elsif I18n.locale == :es 
		          					@job.title_es
		          				end
		          			%>
	          			</p>
	          			<p class="video-year">
          					<% if @job.year.present? %>
	          					(<%= @job.year %>)
		          			<% end %>
      					</p>
	          		</div>
	          		<div class="col-xs-2" style="display: block">
	          			<%= image_tag("fechar_popup.svg", alt: t('home.close_popup'), title: t('home.close_popup'), height: 32, width: 32, class: "close", data: {dismiss: "modal", label: "Close"}) %>
	          		</div>
	          	</div>
	          </div>

	          <div class="col-xs-12 col-video">
            	<div class="embed-container">
            		<iframe class="video" src="//player.vimeo.com/video/<%= @job.video_id %>" frameborder="0" webkitAllowFullScreen mozallowfullscreen allowFullScreen></iframe>
            	</div>
	          </div>

	          <div class="col-xs-12 col-video-description">
	          	<!-- <div class="row synopsis"> -->
		          	<!-- Synopsis -->
		          	<div class="row">
		          		<div class="col-xs-12 col-sm-6 synopsis">
		          			<p class="description-title">
		          				<%= t('home.job.synopsis') %>
		          			</p>
		          			<p class="description-text">
		          				<%= if I18n.locale == :en 
		          						@job.synopsis_en
		          					elsif I18n.locale == :pt 
		          						@job.synopsis_pt
		          					elsif I18n.locale == :es
		          						@job.synopsis_es
		          					end
		          				%>
		          			</p>
		          		</div>
			          	<!-- </div> -->

			          	<!-- Infos / Datasheet -->
		          		<div class="col-xs-12 col-sm-6 datasheet">
		          		<p class="description-title"><%= t('home.job.info') %></p>
		          		<table class="table table-striped description-text table-hover"><tbody>

          					<!-- Title -->
          					<% if @job.title? %>
	          					<tr>
	          						<td><%= t('home.job.title') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.title_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.title_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.title_es, {}, wrapper_tag: "span")
		          							end
	          							%>
	          						</td>
	          					</tr>
          					<% end %>

          					<!-- Format -->
          					<% if @job.format_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.format') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.format_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.format_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.format_es, {}, wrapper_tag: "span")
		          							end
	          							%>
	          						</td>
	          					</tr>
		          			<% end %>

		          			<!-- Genre -->
		          			<% if @job.genre_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.genre') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.genre_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.genre_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.genre_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Subgenre -->
	          				<% if @job.subgenre_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.subgenre') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.subgenre_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.subgenre_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.subgenre_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Running Time -->
	          				<% if @job.running_time_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.running_time') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.running_time_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.running_time_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.running_time_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Target -->
	          				<% if @job.target_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.target') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.target_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.target_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.target_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Created By -->
	          				<% if @job.created_by_en.present? %>
	          					<tr> 
	          						<td class="text-nowrap"><%= t('home.job.created_by') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.created_by_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.created_by_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.created_by_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Written By -->
	          				<% if @job.written_by_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.written_by') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.written_by_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.written_by_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.written_by_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>
	          				
	          				<!-- Directed By -->
	          				<% if @job.directed_by_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.directed_by') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.directed_by_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.directed_by_pt, {},wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.directed_by_es, {},wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Status -->
	          				<% if @job.status_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.status') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.status_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.status_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.status_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Player -->
	          				<% if @job.player.present? %>
	          					<tr> 
	          						<td><%= t('home.job.player') %></td>
	          						<td><%= simple_format(@job.player, {}, wrapper_tag: "span") %></td>
	          					</tr>
	          				<% end %>

		          		</tbody></table>
						</div>
					</div>
	          </div>
	        </div>




	      	<!-- Medium / Large devices -->
	        <div class="row hidden-xs hidden-sm hidden-ms">
	          <div class="col-md-8 col-video">
	          	<div class="embed-container">
            		<iframe class="video" src="//player.vimeo.com/video/<%= @job.video_id %>" frameborder="0" webkitAllowFullScreen mozallowfullscreen allowFullScreen></iframe>
            	</div>
	          </div>
	          <div class="col-md-4 col-video-description">

	          	<!-- Meta information -->
	          	<div class="row meta">
	          		<div class="col-md-10">
	          			<p class="video-title">
	          				<%= if I18n.locale == :en 
		          					@job.title_en
		          				elsif I18n.locale == :pt
		          					@job.title_pt
		          				elsif I18n.locale == :es 
		          					@job.title_es
		          				end
		          			%>
	          			</p>
	          			
          				<p class="video-year">
          					<% if @job.year.present? %>
	          					(<%= @job.year %>)
		          			<% end %>
      					</p>
	          		</div>
	          		<div class="col-md-2">
	          			<%= image_tag("fechar_popup.svg", alt: t('home.close_popup'), title: t('home.close_popup'), height: 32, width: 32, class: "close", data: {dismiss: "modal", label: "Close"}) %>
	          		</div>
	          	</div>

		          	<!-- Synopsis -->
		          	<div class="row synopsis">
		          		<div class="col-md-12">
		          			<p class="description-title">
		          				<%= t('home.job.synopsis') %>
		          			</p>
		          			<p class="description-text">
		          				<%= if I18n.locale == :en 
		          						@job.synopsis_en
		          					elsif I18n.locale == :pt 
		          						@job.synopsis_pt
		          					elsif I18n.locale == :es
		          						@job.synopsis_es
		          					end
		          				%>
		          			</p>
		          		</div>
		          	</div>

		          	<!-- Infos / Datasheet -->
		          	<div class="row datasheet">
		          		<div class="col-md-12">
		          		<p class="description-title"><%= t('home.job.info') %></p>
		          		<table class="table table-striped description-text table-hover"><tbody>

		          			<!-- Title -->
          					<% if @job.title? %>
	          					<tr>
	          						<td><%= t('home.job.title') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.title_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.title_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.title_es, {}, wrapper_tag: "span")
		          							end
	          							%>
	          						</td>
	          					</tr>
          					<% end %>

          					<!-- Format -->
          					<% if @job.format_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.format') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.format_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.format_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.format_es, {}, wrapper_tag: "span")
		          							end
	          							%>
	          						</td>
	          					</tr>
		          			<% end %>

		          			<!-- Genre -->
		          			<% if @job.genre_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.genre') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.genre_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.genre_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.genre_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Subgenre -->
	          				<% if @job.subgenre_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.subgenre') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.subgenre_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.subgenre_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.subgenre_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Running Time -->
	          				<% if @job.running_time_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.running_time') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.running_time_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.running_time_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.running_time_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Target -->
	          				<% if @job.target_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.target') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.target_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.target_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.target_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Created By -->
	          				<% if @job.created_by_en.present? %>
	          					<tr> 
	          						<td class="text-nowrap"><%= t('home.job.created_by') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.created_by_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.created_by_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.created_by_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Written By -->
	          				<% if @job.written_by_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.written_by') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.written_by_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.written_by_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.written_by_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>
	          				
	          				<!-- Directed By -->
	          				<% if @job.directed_by_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.directed_by') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.directed_by_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.directed_by_pt, {},wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.directed_by_es, {},wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Status -->
	          				<% if @job.status_en.present? %>
	          					<tr> 
	          						<td><%= t('home.job.status') %></td>
	          						<td>
	          							<%= if I18n.locale == :en 
	          									simple_format(@job.status_en, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :pt 
		          								simple_format(@job.status_pt, {}, wrapper_tag: "span")
		          							elsif I18n.locale == :es
		          								simple_format(@job.status_es, {}, wrapper_tag: "span")
		          							end
	          							%>
          							</td>
	          					</tr>
	          				<% end %>

	          				<!-- Player -->
	          				<% if @job.player.present? %>
	          					<tr> 
	          						<td><%= t('home.job.player') %></td>
	          						<td><%= simple_format(@job.player, {}, wrapper_tag: "span") %></td>
	          					</tr>
	          				<% end %>

		          		</tbody></table>
		          		</div>
					</div>
	          </div>
	        </div>
	      </div>
	    </div><!-- /.modal-content -->
  	</div><!-- /.modal-dialog -->
</div><!-- /.vertical alignment-helper -->
