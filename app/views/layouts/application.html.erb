<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
<head>

	<!-- Meta tags -->
  	<% set_meta_tags description: "Intro Pictures official website." %>
    <% set_meta_tags keywords: %w[intro pictures intropictures website] %>
  	<% set_meta_tags canonical: url_for(root_url) %>
  	<% set_meta_tags alternate: { "pt" => url_for(root_url) + "pt",
                           		 "en" => url_for(root_url) + "en",
                           		 "es" => url_for(root_url) + "es" } %>
  	<%= display_meta_tags site: 'Intro Pictures' %>

  	<meta name="viewport" content="width=device-width, initial-scale=1">
  	<%= favicon_link_tag 'favicon.png' %>
  	<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  	<%= csrf_meta_tags %>

  	
             

</head>
<body>

<%= yield %>

<!-- Maybe it's needed by Rails / ActiveAdmin -->
<!-- <p class="notice hidden-xs"><%= notice %></p> -->
<!-- <p class="alert hidden-xs"><%= alert %></p> -->

</body>
</html>
